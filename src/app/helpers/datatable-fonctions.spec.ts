import { DatatableFonctions } from './datatable-fonctions';

describe('DatatableFonctions', () => {
  it('should create an instance', () => {
    expect(new DatatableFonctions()).toBeTruthy();
  });
});
